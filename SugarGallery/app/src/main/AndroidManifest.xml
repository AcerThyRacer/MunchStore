<?xml version="1.0" encoding="utf-8"?>
<!-- SugarGallery - Android Manifest -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Storage permissions for accessing media -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Camera permission for taking photos -->
    <uses-permission android:name="android.permission.CAMERA"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />

    <!-- Vibration for haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Set wallpaper permission -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <!-- Request install packages for P2P sharing (optional) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Feature declarations -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".SugarGalleryApplication"
        android:allowBackup="true"
        android:appCategory="image"
        android:icon="@mipmap/ic_launcher_sugar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_sugar_round"
        android:supportsRtl="true"
        android:theme="@style/SugarGalleryTheme"
        android:requestLegacyExternalStorage="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:theme="@style/SugarGalleryTheme.Splash"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Media Viewer Activity -->
        <activity
            android:name=".ui.activities.MediaViewerActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme.Dialog.Fullscreen"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTask" />

        <!-- Media Editor Activity -->
        <activity
            android:name=".ui.activities.MediaEditorActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTask" />

        <!-- Recycle Bin Activity -->
        <activity
            android:name=".ui.activities.RecycleBinActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme"
            android:label="@string/recycle_bin_settings"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Hidden Folders Activity -->
        <activity
            android:name=".ui.activities.HiddenFoldersActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme"
            android:label="@string/hidden_folders"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Theme Picker Activity -->
        <activity
            android:name=".ui.activities.ThemePickerActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme"
            android:label="@string/choose_sugar_theme"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme"
            android:label="@string/settings"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Folder Selection Activity -->
        <activity
            android:name=".ui.activities.FolderSelectionActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme"
            android:label="@string/folders" />

        <!-- Album Activity -->
        <activity
            android:name=".ui.activities.AlbumActivity"
            android:exported="false"
            android:theme="@style/SugarGalleryTheme"
            android:label="@string/albums" />

        <!-- Intent Filters for viewing media -->
        <activity-alias
            android:name=".ViewImageActivity"
            android:exported="true"
            android:targetActivity=".ui.activities.MediaViewerActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".ViewVideoActivity"
            android:exported="true"
            android:targetActivity=".ui.activities.MediaViewerActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity-alias>

        <!-- Intent Filters for sharing media to SugarGallery -->
        <activity
            android:name=".ui.activities.ImportActivity"
            android:exported="true"
            android:theme="@style/SugarGalleryTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- File Provider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Image cache provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.cache"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/cache_paths" />
        </provider>

    </application>

</manifest>
